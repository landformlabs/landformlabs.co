# Strava API Configuration
# Required for Strava integration functionality
# Get these values by creating a Strava API application at: https://www.strava.com/settings/api

# Strava Client ID (public)
NEXT_PUBLIC_STRAVA_CLIENT_ID=your_strava_client_id_here

# Strava Client Secret (private - keep secure)
STRAVA_CLIENT_SECRET=your_strava_client_secret_here

# Strava OAuth Redirect URI
# This should match the redirect URI configured in your Strava app settings
# For local development: http://localhost:3000/api/strava/callback
# For production: https://yourdomain.com/api/strava/callback
NEXT_PUBLIC_STRAVA_REDIRECT_URI=http://localhost:3000/api/strava/callback

# Node Environment
# Set to "production" for production builds
NODE_ENV=development

# Next.js Configuration
# Optional: Set a custom port for local development
# PORT=3000

# Security Configuration (Production)
# Uncomment and configure for production deployment
# NEXTAUTH_SECRET=your_secure_random_string_here
# NEXTAUTH_URL=https://yourdomain.com

# Analytics (Optional)
# NEXT_PUBLIC_GA_ID=your_google_analytics_id_here

# Rate Limiting (Optional)
# Configure if you need custom rate limiting
# STRAVA_RATE_LIMIT_PER_MINUTE=100
# STRAVA_RATE_LIMIT_PER_HOUR=1000
